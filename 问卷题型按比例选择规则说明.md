# 问卷题型按比例选择规则说明

## 一、比例格式说明

比例中的数字代表**某个选项占总选择次数的百分比**。

基本格式：

```text
题号:选项1比例,选项2比例,选项3比例,...
```

例如：

```text
1:60,40
```

表示第 1 题的第 1 个选项占 60%，第 2 个选项占 40%。

> **注意：**
> - 冒号前为题号。
> - 冒号后按照选项顺序填写各选项的比例。
> - 比例必须与题目选项顺序一一对应。
> - 有几个选项，就必须填写几个比例数字。
> - 题号按照问卷从上到下的实际顺序计算，不一定等于问卷页面上标注的题号。

---

## 二、不同题型的比例规则

| 题型 | 比例总和 | 比例填写规则 |
|---|---:|---|
| 单选题 | **= 100** | 每个选项填写一个百分比 |
| 多选题 | **> 100** | 每个选项填写一个百分比，各选项相互独立 |
| 量表题 | **= 100** | 按单选题规则处理 |
| 矩阵题 | **= 100** | 每个矩阵小题分别设置比例 |
| 填空题 | 不设置比例 | 根据是否必填进行设置 |

---

## 三、单选题

### 3.1 基本规则

单选题每道题有几个选项，就必须填写几个比例数字。

**所有选项比例之和必须等于 100。**

例如，第 1 题有两个选项：

```text
1:60,40
```

表示：

- 第 1 个选项：60%
- 第 2 个选项：40%

计算：

```text
60 + 40 = 100
```

符合要求。

### 3.2 三个选项示例

```text
2:20,30,50
```

表示：

- 第 1 个选项：20%
- 第 2 个选项：30%
- 第 3 个选项：50%

比例总和：

```text
20 + 30 + 50 = 100
```

### 3.3 不选择某个选项

如果不希望某个选项被选择，可以将该选项比例设置为 `0`。

例如：

```text
3:50,50,0
```

表示：

- 第 1 个选项：50%
- 第 2 个选项：50%
- 第 3 个选项：0%

---

## 四、多选题

### 4.1 基本规则

多选题与单选题不同。

多选题中，一个用户可以同时选择多个选项，因此**各选项的选择比例可以相互叠加**。

因此：

> **多选题所有选项比例之和必须大于 100。**

例如，第 15 题有 5 个选项：

```text
15:87,23,43,51,38
```

表示：

- A 选项：87%
- B 选项：23%
- C 选项：43%
- D 选项：51%
- E 选项：38%

比例总和：

```text
87 + 23 + 43 + 51 + 38 = 242
```

因为：

```text
242 > 100
```

所以符合多选题的比例规则。

### 4.2 不选择某个选项

如果某个选项不希望被选择，可以设置为 `0`。

例如：

```text
15:87,0,43,51,38
```

表示第 15 题的 B 选项不会被选择。

---

## 五、量表题

### 5.1 基本规则

量表题按照**单选题规则**处理。

因此：

> **量表题所有等级的比例之和必须等于 100。**

量表题有几个等级，就必须填写几个比例。

### 5.2 五级量表示例

例如一道五级量表题：

```text
5:10,20,30,25,15
```

表示：

| 等级 | 比例 |
|---|---:|
| 第 1 级 | 10% |
| 第 2 级 | 20% |
| 第 3 级 | 30% |
| 第 4 级 | 25% |
| 第 5 级 | 15% |

比例总和：

```text
10 + 20 + 30 + 25 + 15 = 100
```

### 5.3 其他量表

同样适用于：

- 四级量表
- 五级量表
- 七级量表
- 九级量表

例如七级量表：

```text
6:10,10,15,20,20,15,10
```

比例总和必须为：

```text
10 + 10 + 15 + 20 + 20 + 15 + 10 = 100
```

---

## 六、矩阵题

### 6.1 基本规则

矩阵题需要特别注意：

> **每一个矩阵小题都需要单独设置比例。**

矩阵题不能只按照整道矩阵题设置一个比例。

例如，第 4 题是一个五级量表矩阵题，包含 3 个矩阵小题：

```text
4.1:20,30,25,25,0
4.2:20,30,25,25,0
4.3:20,30,25,25,0
```

其中：

- `4.1`：矩阵题第 1 个小题
- `4.2`：矩阵题第 2 个小题
- `4.3`：矩阵题第 3 个小题

每一个小题都需要单独满足比例要求。

例如：

```text
20 + 30 + 25 + 25 + 0 = 100
```

因此 `4.1` 符合要求。

`4.2`、`4.3` 同样需要分别满足：

```text
比例总和 = 100
```

### 6.2 矩阵题题号格式

矩阵题必须使用：

```text
主题号.小题号
```

例如：

```text
4.1
4.2
4.3
```

不能只写：

```text
4
```

否则无法明确每组比例对应哪个矩阵小题。

---

## 七、填空题

填空题不需要设置选项比例。

如果是非必填的填空题，可以使用：

```text
5:非必填
```

表示第 5 题为非必填填空题。

### 7.1 允许填空的选项

如果某个选择题选项允许用户进行填空，并且该选项不需要按照普通选项进行比例选择，则：

> **允许填空的选项比例设置为 `0`。**

例如：

```text
7:30,30,40,0
```

其中第 4 个选项为允许填空的选项，则该选项比例设置为 `0`。

---

## 八、选项比例为 0 的情况

如果某个选项不希望被选择，可以直接将比例设置为 `0`。

例如：

```text
8:50,0,50
```

表示：

- 第 1 个选项：50%
- 第 2 个选项：0%
- 第 3 个选项：50%

需要注意：

> **比例为 0 的选项仍然必须保留对应的数字，不能直接删除。**

例如有 3 个选项：

```text
8:50,0,50
```

是正确的。

不能写成：

```text
8:50,50
```

因为这样只有两个比例，无法与三个选项对应。

---

## 九、题号规则

题号指的是：

> **问卷从上到下排列后，该题实际处于第几题。**

不一定是问卷页面上显示的题号。

例如问卷实际顺序为：

```text
第 1 题：性别
第 2 题：年龄
第 3 题：职业
第 4 题：满意度
```

则比例分别使用：

```text
1:...
2:...
3:...
4:...
```

如果问卷中的题目存在页面显示编号与实际顺序不一致的情况，应以**问卷从上到下的实际题目顺序**为准。

---

## 十、完整示例

### 10.1 单选题

```text
1:50,50
2:20,30,50
3:50,50,0
```

规则：

```text
每道题比例总和 = 100
```

---

### 10.2 多选题

```text
4:87,23,43,51,38
```

规则：

```text
87 + 23 + 43 + 51 + 38 = 242 > 100
```

符合多选题要求。

---

### 10.3 五级量表题

```text
5:10,20,30,25,15
```

规则：

```text
10 + 20 + 30 + 25 + 15 = 100
```

---

### 10.4 矩阵题

```text
6.1:20,30,25,25,0
6.2:20,30,25,25,0
6.3:10,20,30,25,15
```

每个矩阵小题分别计算比例总和：

```text
6.1 → 100
6.2 → 100
6.3 → 100
```

---

### 10.5 填空题

```text
7:非必填
```

---

## 十一、最终判断规则

可以将所有题型归纳为以下规则：

```text
单选题
→ 每个选项填写一个比例
→ 比例数量 = 选项数量
→ 比例总和 = 100

多选题
→ 每个选项填写一个比例
→ 比例数量 = 选项数量
→ 比例总和 > 100

量表题
→ 按单选题处理
→ 每个等级填写一个比例
→ 比例总和 = 100

矩阵题
→ 每个矩阵小题分别填写比例
→ 每个小题的比例数量 = 该小题选项/等级数量
→ 每个小题比例总和 = 100

填空题
→ 不设置选择比例
→ 按是否必填进行设置

特殊情况
→ 不想选择的选项，比例 = 0
→ 允许填空的选项，比例 = 0
→ 不能遗漏比例
→ 不能多填写比例
→ 比例顺序必须与选项顺序一致
```

## 十二、比例格式快速参考

| 场景 | 示例 | 要求 |
|---|---|---|
| 两个选项单选题 | `1:60,40` | 总和 = 100 |
| 三个选项单选题 | `2:20,30,50` | 总和 = 100 |
| 禁止某选项 | `3:50,50,0` | 0 表示不选择 |
| 多选题 | `4:87,23,43,51,38` | 总和 > 100 |
| 五级量表 | `5:10,20,30,25,15` | 总和 = 100 |
| 矩阵题 | `6.1:20,30,25,25,0` | 每个小题单独计算 |
| 填空题 | `7:非必填` | 不设置比例 |

## 十三、最重要的注意事项

1. **题号必须按照问卷从上到下的实际顺序确定。**
2. **有几个选项，就必须填写几个比例。**
3. **比例顺序必须与选项顺序一致。**
4. **单选题比例总和必须为 100。**
5. **多选题比例总和必须大于 100。**
6. **量表题按照单选题规则，比例总和必须为 100。**
7. **矩阵题必须按照 `题号.小题号` 的格式分别设置比例。**
8. **矩阵题每个小题的比例总和必须为 100。**
9. **不希望选择的选项比例设置为 0。**
10. **允许填空的选项比例设置为 0。**
11. **不能因为某个选项比例为 0 就省略该比例数字。**
12. **最终生成比例后，应再次检查比例数量、比例总和以及题号格式。**