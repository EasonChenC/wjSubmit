你是问卷分析专家。请对以下题目执行三个分析任务。

## 任务一：识别量表题 vs 基础题

**量表题**（Likert scale / 评分量表）：
- 用于测量连续维度：态度、满意度、频率、程度、强度等
- 选项通常呈梯度排列：表示从"最低"到"最高"的程度
- 常见选项词汇：非常/很/比较/一般/不太/完全... + 满意/同意/同意/强烈等
- 或数字梯度：1=最低，5=最高

**基础题**（分类题）：
- 用于收集信息分类：性别、年龄、职业、地区等
- 选项无序或无量化含义：男/女，不同年龄段，不同职业等
- 选项之间无"高低优劣"之分

## 任务二：识别正向题与反向题（仅对量表题）

**正向题**：
- 题干和选项的"积极方向"一致
- 答题者选择更靠近"积极端"的
选项表示态度更积极
- 例："您对产品的满意度" + 选项"非常满意...非常不满意" → 选"非常满意"=积极

**反向题**：
- 题干为负向表述，需要反向编码（在数据分析时）
- 答题者选择更靠近"消极端"的选项才表示对题干的同意
- 例："您对产品感到失望" + 选项"非常同意...非常不同意" → 选"非常同意"=消极态度

## 任务三：识别每道量表题中的积极/消极选项值（仅对量表题）

根据题干文本+选项标签，判断：
- `positive_values`：表示积极态度/满意/同意/频繁/高满意度 等的选项value列表
- `negative_values`：表示消极态度/不满/不同意/很少/低满意度 等的选项value列表
- `neutral_values`：中立选项的value列表

**重要**：不能只靠选项在列表中的位置判断。必须通过选项标签文本理解其含义。
- 如"非常满意,比较满意,一般,不太满意,非常不满意" → positive=[1,2], neutral=[3], negative=[4,5]
- 但如果选项反序："非常不满意,不太满意,一般,比较满意,非常满意" → positive=[4,5], neutral=[3], negative=[1,2]

## 输入数据

题目数据格式：
```json
{
  "id": "题目ID",
  "type": "题型(radio/rating/nps/matrix)",
  "label": "题目文本",
  "options": [
    {"value": "1", "label": "选项文本"},
    {"value": "2", "label": "选项文本"}
  ]
}
```

题目数据：
{questions_json}

## 输出格式

**只返回JSON，不包含任何说明文字。** 格式如下：

```json
{{
  "scale_items": [
    {{
      "id": "题目ID",
      "is_reverse": false,
      "positive_values": ["1", "2"],
      "negative_values": ["4", "5"],
      "neutral_values": ["3"]
    }}
  ],
  "non_scale_ids": ["q1", "q2", "q3"]
}}
```

其中：
- `scale_items`：所有被识别为量表题的题目
- `non_scale_ids`：被识别为基础题/分类题的题目ID列表
